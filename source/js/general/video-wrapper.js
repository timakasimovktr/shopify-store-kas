import CustomVideo from '../helpers/customVideo';

$(document).ready(function() {
  const $videoWrapper = $('.quote-item .video-holder');
  const $videoElement = document.querySelector('.video-holder__video-element');

  if ($videoWrapper.length > 0) {
    if($('#video-holder__video').length > 0) {
      // document.getElementById('product_video').play();
    }
  }
});